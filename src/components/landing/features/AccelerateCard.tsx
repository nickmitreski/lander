import MonetiseImages from "./MonetiseImages";

const AccelerateCard = () => {
  return (
    <div className="w-full h-full flex items-center justify-center relative overflow-hidden">
      <MonetiseImages />
    </div>
  );
};

export default AccelerateCard; 